import { IUserInfo } from "@/store/user_info";

async function storePwd(info: IUserInfo) {
  const userInfo = await encrypt
}


const Login: React.FC = () => {
  return <div>Login</div>;
};

export default Login;
